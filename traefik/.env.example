# Traefik Configuration
# Copy this file to .env and update with your values

# Your domain name (without protocol)
DOMAIN=example.com

# Email for Let's Encrypt certificate notifications
ACME_EMAIL=your-email@example.com

# Cloudflare DNS Challenge (optional, only if using DNS challenge)
# Required if your server is behind a firewall or for wildcard certificates
CF_API_EMAIL=your-cloudflare-email@example.com
CF_API_KEY=your-cloudflare-global-api-key

# Traefik Dashboard Credentials
# Generate with: echo $(htpasswd -nb admin yourpassword) | sed -e s/\\$/\\$\\$/g
# Default: admin/changeme (CHANGE IN PRODUCTION!)
TRAEFIK_DASHBOARD_USER=admin
TRAEFIK_DASHBOARD_PASSWORD_HASH=$$apr1$$8evjzVMJ$$sZbT7CqVf0TfIVeJJJ0GC.

# Environment
TRAEFIK_ENV=production
